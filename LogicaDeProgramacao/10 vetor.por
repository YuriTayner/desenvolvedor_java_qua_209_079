programa {
  funcao inicio() {
    // 
      cadeia nomes [] = {"Douglas", "Fulano", "Maria", "Cicrano"}

    // exibe na tela
    /*escreva (nomes[0], "\n")
    escreva (nomes[2], "\n")*/

    // exibe na tela a lista de nomes
    para (inteiro i = 0; i < 4; i++ ) {
      escreva (nomes[i], "\n")
    }
  }
}
